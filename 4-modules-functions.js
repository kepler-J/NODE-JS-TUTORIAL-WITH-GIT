const display = (mesage) => {
  console.log(`Message=${mesage}`);
};

module.exports.a = display;

const display2 = (mesage) => {
  console.log(`Message=====${mesage}`);
};

module.exports.b = display2;
